// TODO: Create types file
import {
  ADD_DEVICE,
  GET_DEVICES,
  DEVICE_ERROR,
  DELETE_DEVICE,
  UPDATE_DEVICE,
  CLEAR_DEVICES,
  SET_CURRENT,
  CLEAR_CURRENT,
  CLEAR_FILTER,
  FILTER_DEVICE,
} from '../types';

export default (state, action) => {
  switch (action.type) {
    // TODO: Add other action types
    default:
      return state;
  }
};
